1. 什么是大语言模型？
大预言模型是一种用于理解、生成和响应类似人类语言文本的神经网络。

2. 构建大语言分为哪些阶段？
大语言模型的构建通常包括预训练(pre-training)和微调(fine-tuning)两个阶段。

3. Transformer由哪些模块组成？
Transformer由两个子模块组成：编码器和解码器。
编码器(encoder)模块负责处理输入文本，将其编码为一系列数值表示或向量，以捕捉输入的上下文信息。
解码器(decoder)模块接受这些向量，并据此生成输出文本。